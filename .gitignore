# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
.env 

# dependencies
/node_modules
/.pnp
.pnp.js

# generated by scripts/init-images.mjs — placeholder product images, not
# source content
/img

# testing
/coverage

# production
/build
/dist

# vite
.vite

# amplify — generated per environment (ampx sandbox locally, ampx
# pipeline-deploy in CI per amplify.yml). Committing it lets it go stale
# and reference resources/regions from a torn-down environment.
amplify_outputs.json
.amplify/

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
